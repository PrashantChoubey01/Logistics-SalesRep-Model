# Copy this file to `.env` and fill in real values. `.env` is git-ignored.

# Required: Databricks personal access token used to authenticate model serving calls.
DATABRICKS_TOKEN=

# Optional: override the values in config/config.json.
DATABRICKS_BASE_URL=https://adb-2252852771922438.18.azuredatabricks.net/serving-endpoints/
MODEL_ENDPOINT_ID=databricks-claude-sonnet-4-6

# Optional: SMTP credentials for outbound email (only needed when email sending is enabled).
SENDER_EMAIL_PASSWORD=
